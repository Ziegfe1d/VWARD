# Router source classification

The archive `VWARD_router_source_20260906-235502.tar.gz` contains 36 files. Its archive SHA-256 and all 34 entries in its internal `SHA256SUMS` were verified before migration.

## Source published

- all 17 files from `/opt/bin`;
- all four VWARD init scripts from `/opt/etc/init.d`;
- `/opt/etc/keenetic-apps/lighttpd.conf`;
- `/opt/share/keenetic-apps/www/index.html`;
- `/opt/share/keenetic-apps/www/cgi-bin/api.cgi`;
- `force-vpn.conf` and `services.conf` as configuration examples;
- the eight production cron entries as `config/cron/root.crontab`.

## Generated

- `/opt/etc/adaptive-route/hints.conf` — generated by `adaptive-hints-update.sh` from `itdoginfo/allow-domains`; not published.

## Local configuration

- `/opt/etc/adaptive-route/skip-domains.conf` — routing policy specific to the source installation; not published as a universal default.

## Runtime/control state

- `wg-failopen.disabled` — snapshot metadata confirms the file was absent. It is not source.

## Snapshot metadata not published as program source

- snapshot inventory and collection notes;
- security-candidate scan output;
- Entware package dump;
- crontab installation-path note;
- snapshot-level `SHA256SUMS`.

The relevant facts from these records are preserved in repository documentation.

## Third-party files

No Entware, AdGuard Home, curl, jq, lighttpd, crond, tcpdump or other third-party binary is published. They remain external dependencies.

## Comparison with the initial GitHub snapshot

- `IDENTICAL`: none of the active router program/configuration files;
- `DIFFERENT`: `web/index.html`;
- `MISSING_FROM_GITHUB`: the other active program files, configuration examples and managed cron source;
- `GITHUB_ONLY`: repository policy/documentation scaffolding and the obsolete UI-only updater placeholder.

GitHub-only documentation was updated where necessary. The obsolete updater placeholder and empty backend placeholder were removed; no initial history was rewritten.
