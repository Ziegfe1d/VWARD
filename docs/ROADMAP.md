# Дорожная карта

## Текущее состояние ветки dev: 0.2.0-rc.1.fix.6

- этап **RC1** (feature complete): новые функции больше не добавляются;
- `0.2.0-rc.1.fix.1`–`.fix.6` — исправления RC1, найденные на реальном роутере
  (BusyBox `stat -c`, порты коммутатора, Ads без настройки, jq без regex, долгие операции Console,
  статус supervisor, Wi-Fi клиенты);
- Console записывает настройки, выбирает туннель, управляет защитой интернета,
  компонентами, Ads, cron и входом по учётной записи Keenetic;
- release rehearsal в CI: настоящий candidate подписывается одноразовым ключом,
  ставится поверх файлов signed beta-пакетов и полностью откатывается;
- signed feed ветки `dev` ещё содержит исторический `0.1.7-beta`: RC1 публикуется
  после GO владельца.

## Этапы

RC1 → RC2 → RP1 → RP2 → RETAIL (`0.2.0`). Условия этапов:
[`MASTER_PLAN_0.2_TO_FINAL.md`](MASTER_PLAN_0.2_TO_FINAL.md); порядок выпуска и приёмки:
[`RELEASE_0.2.0.md`](RELEASE_0.2.0.md).

Высокорисковое переименование runtime-файлов, init-команд и state paths отложено до
отдельной совместимой миграции. Новый release/tag в рамках документационных и UI-правок
не создаётся.

## Линии разработки

- `beta` - линия `0.1.9-beta`, установленная на рабочем роутере (feed ветки `main`).
- `dev` - линия `0.2.0`: сейчас `0.2.0-rc.1.fix.6`, далее RC2, RP1, RP2 и RETAIL.
- Проверенные изменения beta-линии переносятся в `dev`, после чего основная разработка
  продолжается в `dev`.
- Исторические подписанные пакеты `0.1.x-dev` не переименовываются задним числом.

Канонический порядок работы: [`MASTER_PLAN_0.2_TO_FINAL.md`](MASTER_PLAN_0.2_TO_FINAL.md).
Остальные документы раскрывают отдельные области, а не заменяют этот план.
