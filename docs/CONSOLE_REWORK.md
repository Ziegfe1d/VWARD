# VWARD Console: список доработок

Рабочий список изменений интерфейса Console по итогам просмотра демо-страницы.
Отмечаем выполненное прямо здесь.

## Правило для всех этапов

Изменение интерфейса правит существующий код, а не дописывает поверх. Новое CSS-правило
заменяет старое; одно и то же свойство одного элемента задаётся в одном месте.

## Статус переноса в настоящую Console

Перенесено (0.2.0-dev, `web/index.html`, `web/assets/vward-console.css`, `web/assets/vward-console.js`):
новая структура из 10 разделов, шапка с поиском и уведомлениями, настраиваемая нижняя
панель, карточки «плитки/список», компактные списки, единые иконки, журналы с экспортом и все
действия, которые уже поддерживает Console API (обновления, сверка маршрутов, IP-категории,
проверка туннеля, Wi-Fi-закрепление, правила/источники/задания/публикация/настройки рекламы,
HTTPS-фильтр, диагностика). Отмечены `[x]`.

Остаются пункты с пометкой «Бэкенд» и то, что от них зависит: там, где API пока не отдаёт
данные или не принимает изменение, Console показывает значение только для чтения и не
предлагает неработающих кнопок.

## Структура разделов

У каждого параметра одно место и одно имя; поиск ведёт прямо к нему.

| Группа | Раздел | Что там |
|---|---|---|
| Главное | Обзор | карточки состояния, настраиваемые |
| Главное | Журналы | все журналы, экспорт |
| Сеть | Интернет | подключение, интерфейс, проверка связи |
| Сеть | VPN | туннели, fail-open |
| Сеть | Маршрутизация | каталог, AdaptiveAuto, группа маршрутизации, проверка адреса |
| Сеть | Wi-Fi клиенты | сбор данных, домашний сегмент, клиенты |
| Сеть | Реклама и трекеры | AdGuard Home, блокировки, источники |
| VWARD | Система | устройство, компоненты, среда выполнения, хранилище |
| VWARD | Обновления | состояние и все настройки обновлений |
| VWARD | Настройки | доступ к Console и вход, нижняя панель |

- «Безопасность» объединена с «Настройками»: вход по паролю задаётся в одном месте.
- Сведения об устройстве и автоматически найденные интерфейсы показаны в своих разделах,
  а не отдельным блоком «только чтение» в «Настройках».
- Тема переключается одной кнопкой в шапке, без дубля в «Настройках».
- Одно имя для одного понятия: «Интернет» вместо смеси «Интернет»/«WAN»; «Система» вместо
  «Платформа»; компоненты названы по-русски и не совпадают с названиями разделов
  («Движок маршрутизации», «Защита интернета», «Контроль Wi-Fi клиентов»).
- Кнопка «Журнал» в карточке компонента открывает нужную вкладку журналов.

## Этап 1. Основа: чистый CSS

- [x] Свести `vward-console.css` к одному набору правил без многослойных переопределений
      (сейчас, например, у кнопок нижней панели около 10 перекрывающих правил).
- [x] Единые токены: цвета светлой и тёмной темы, шрифты, размеры, отступы, радиусы, тени.
- [x] Проверить выравнивание текста, шрифты, анимации, переходы, заливки, линии; убрать
      наслоения текста.
- [x] Регрессионная проверка: одно свойство одного селектора не переопределяется повторно.

## Этап 2. Иконки

- [x] Перерисовать все иконки в едином формате и стиле: одна сетка, толщина линии,
      скругления (обновление, тема, уведомления, поиск, навигация, действия).

## Этап 3. Шапка и навигация

- [x] Убрать синий блок статуса на главном экране.
- [x] Колокольчик уведомлений справа в шапке: спокойный, когда всё в порядке; счётчик
      при неполадках; по нажатию - список проблем со ссылкой на нужный раздел.
- [x] Общий поиск значком рядом с колокольчиком: ищет по всей Console (разделы, настройки,
      компоненты) и переходит к найденному; убрать поля поиска со страниц.
- [x] Компактные кнопки шапки на узком экране.
- [x] Нижняя панель на телефоне: скруглённая «плавающая» форма сохраняется, фон непрозрачный,
      контент не уходит под панель, подписи без обрезки, подложка активного пункта внутри панели.
- [x] Настройка нижней панели: выбор до 4 разделов и их порядка, остальные - в «Ещё».
- [x] «Назад к обзору» не уезжает под верх экрана.

## Этап 4. Содержимое страниц

- [x] Карточки обзора: метки статуса не выходят за край, нет залипающей рамки после
      касания, иконка выровнена по верху с текстом, одинаковое описание на телефоне и
      компьютере, компактная высота.
- [x] «Настроить карточки» - компактная кнопка или пункт меню, без отдельной строки.
- [x] Характеристики на всех страницах - компактный список «название - значение» с тонкими
      разделителями вместо сетки плиток; цветная метка только там, где есть состояние.
- [x] Без вложенности «карточка в карточке в карточке».
- [x] Понятные русские метки вместо «READ ONLY», «ТОЛЬКО VWARD» и подобных.
- [x] Любое число, за которым стоит список («3 домена», «214 маршрутов», «11 компонентов»),
      кликабельно и открывает этот список; «назад» в шапке возвращает в раздел.
- [x] Подсказка под названием параметра объясняет, а не повторяет значение или состояние
      («работает» не пишется и в подсказке, и в значении).
- [x] Адреса веб-интерфейсов - внешние ссылки: AdGuard Home в «Рекламе», веб-интерфейс
      Keenetic в «Системе» (вернуть кнопку «Keenetic ↗» из старой Console).
- [x] Значение, которое относится к другому разделу, ведёт в него («Версия VWARD» -
      в «Обновления», «Туннель для маршрутов» - в «VPN»); «Заблокировано запросов» открывает
      журнал запросов сразу с фильтром «Заблокированные».
- [x] Строки «последнее событие» («Последняя сверка», «Последний сбор», «Последняя проверка»,
      «Проверка туннеля», «Служебная очистка») открывают журнал на нужной вкладке; строки
      «Диагностики» ведут в свой раздел, «Задания по расписанию» - в карточку компонента.
- [x] Подпись строки не повторяет её метку состояния («не помогло» + «Не помогло»).
- [x] Журналы: кнопки «Копировать», «Поделиться» (системное меню, где браузер его даёт),
      «Сохранить .txt» и «Все журналы» одним файлом; перенос длинных строк включён по
      умолчанию и переключается; вкладки журналов переносятся, а не уходят за край.
- [x] Страница не выделяет текст и не показывает меню «Копировать» при долгом нажатии;
      выделение остаётся только в полях ввода и журналах.

## Этап 4а. Настройки обновлений в разделе «Обновления»

- [x] Прямо на странице один выбор «Установка обновлений»: «Автоматически» (сразу),
      «По расписанию» (в окно «с - до», критические исправления - сразу) или «Вручную»
      (только проверка и уведомление). Окно показывается только для «По расписанию».
- [x] Окно «с - до» и интервал проверки выбираются на странице.
- [x] Там же канал «Бета»/«Dev» с предупреждением про dev.
- [x] Изменения сохраняются сразу при выборе, как в остальных разделах; неверное окно
      (начало = конец) не сохраняется.
- [x] Кнопки «Проверить» и «Откатить» одинаковой ширины в одну строку.
- [x] Бэкенд: безопасная запись `safe_window_start/end` и `check_interval_seconds` с проверкой
      значений и журналом аудита (`vward-console-config.sh`).
- [x] Бэкенд: «Автоматически» = `auto_apply=1` без ограничения окном (нужна поддержка в
      Update Engine); запись `channel`.

## Этап 4б. Реклама и трекеры: полное управление без AdGuard Home

- [x] Главная страница раздела: переключатель блокировки (пауза/возобновление), состояние
      AdGuard Home, статистика за сутки; ссылки на списки; публикация; проверка домена;
      настройки (режим «По расписанию»/«По запросам», интервал, автообновление источников,
      область новых правил).
- [x] Журнал запросов с фильтром (все / заблокированные / разрешённые / на проверке) и
      кнопками «Разрешить»/«Заблокировать» у каждой строки.
- [x] «На проверке»: решение по каждому домену одной кнопкой.
- [x] «Заблокировано»: поиск и «Разрешить».
- [x] «Мои правила»: добавить домен (блокировать/разрешить, только домен/с поддоменами),
      удалить правило.
- [x] «Источники»: режим каждого источника «Выключен / Проверка / Активен»; добавить свой
      источник по https-адресу (сначала в режиме «Проверка»), удалить свой.
- [x] «Категории блокировки»: переключатели категорий.
- [x] «Задания»: текущее, очередь, последнее; «Обновить источники», «Пересобрать правила».
- [x] Публикация в AdGuard Home: счётчик неопубликованных изменений, «Опубликовать» с
      подтверждением на странице, переключатель автопубликации.
- [x] Строки списков на телефоне: статус цветным словом в подписи, действия иконками,
      домены переносятся только по точкам.
- [x] Бэкенд: журнал запросов AdGuard Home в Console API; статистика за сутки; запись
      категорий и режима работы; пользовательские источники (сейчас список источников
      фиксирован в `source-registry.json`) с проверкой адреса, размера и формата.

## Этап 4в. VPN и маршрутизация

- [x] VPN: список туннелей (состояние, handshake, трафик), у каждого - карточка с сервером,
      адресом, MTU; туннель для маршрутов отмечен в подписи.
- [x] «Использовать для маршрутов» в карточке туннеля с подтверждением на странице
      (с предупреждением, если туннель не в сети).
- [x] Защита VPN: переключатель (выключение - с подтверждением на странице), состояние
      fail-open, частота проверки, «Проверить» и «Журнал».
- [x] Защита VPN: частота восстановления.
- [x] Маршрутизация: сводка (туннель, домены, маршруты, группа); «Что идёт через VPN» -
      мои домены, «всегда через VPN», AdaptiveAuto, категории доменов, IP-категории,
      проверяемые сервисы; проверка адреса.
- [x] Мои домены и «Всегда через VPN»: добавить и убрать домен.
- [x] AdaptiveAuto: закрепить домен в «Мои домены» или убрать.
- [x] Категории доменов - переключателями.
- [x] IP-категории - переключателями; количество доменов/сетей у категорий.
- [x] Обслуживание: последняя сверка, «Сверить маршруты» и «Обновить IP-категории» с
      подтверждением на странице.
- [x] Настройки: AdaptiveAuto вкл/выкл, автоопределение категории новых доменов.
- [x] Бэкенд: Защита VPN через `tunnel-guard.disabled`; группа «Мои домены» в конфигурации
      Keenetic, `force-vpn.conf`, AdaptiveAuto (закрепить/убрать), флаги `categories.tsv` -
      через Console API (`config-data`, `config`) с проверкой значений, резервной копией,
      проверкой результата и журналом аудита.
- [x] Бэкенд: переключение туннеля для маршрутов - маршруты групп в Keenetic,
      `device.conf`, перенос IP-маршрутов policy-sync, откат при ошибке.
- [ ] Проверить на роутере синтаксис `dns-proxy route object-group` / `no route object-group`.
- [x] Бэкенд: активные IP-категории, `CLASSIFIER_ENABLED`; статистика трафика туннелей из RCI.

## Этап 4г. Интернет, Wi-Fi клиенты, Система, Настройки, Обзор

- [x] Интернет: подключение (кабель, IPv4, шлюз, DNS), «Проверить» и «Журнал»; Защита
      интернета - переключатель, порядок восстановления по шагам, текущее состояние, история
      восстановлений; «Обновить адрес» и «Переподключить» с подтверждением на странице.
- [x] Wi-Fi клиенты: переключатели «Сбор данных» и «Ручное управление»; «Автоисправление»
      заблокировано до проверки на роутере; карточка клиента с выбором «Авто / Только 2.4 /
      Только 5 ГГц» (только при включённом ручном управлении) и подтверждением; пороги
      анализа (окно, частые переходы, слабый 5 ГГц, число слабых замеров).
- [x] Система: устройство и время работы; компоненты, задания по расписанию, диагностика
      (вернуть из старой Console) со ссылками; хранилище и «Очистить временные файлы».
- [x] Настройки: «Сбросить вид Console» (карточки, нижняя панель, тема в этом браузере).
- [x] Обзор: карточка «Реклама» - у каждого раздела сети есть карточка.
- [x] Бэкенд: включение/выключение Защиты интернета (`wan-guard.disabled`); ручные «Обновить
      адрес» и «Переподключить» через Console API (не чаще раза в минуту, отдельно от лимитов
      автоматики); история восстановлений из журнала; запись `ENABLED`, `CONTROL_ENABLED` и
      порогов Wi-Fi Client Guard.
- [x] Бэкенд: состояние cron-заданий и запуск служебной очистки через API.

## Этап 5. Компоненты: карточки и отключение

- [x] Из списка компонентов - переход в карточку компонента: описание, состояние, версия,
      расписание запуска, журнал, ссылка на раздел, «Зависит от» и «От него зависят».
- [x] Переключатель «Компонент включён» с подтверждением прямо на странице: перед
      отключением показать, какие компоненты остановятся вместе с ним (и какие продолжат
      работать на устаревших данных); при включении - какие включатся вместе с ним.
- [x] Базовые компоненты (Ядро платформы, Среда выполнения, Console, Обновления) отключить
      нельзя - с объяснением причины.
- [x] Бэкенд: механизм включения/отключения компонента - флаг
      `/opt/etc/vward/components/<id>.disabled`, проверка `vward_component_gate` во всех
      точках входа (cron, supervisor, init-скрипт движка, ручные инструменты), операция
      `component` в Console API с каскадом.
- [x] Бэкенд: полный граф зависимостей в `component-registry.json`: `depends_on` (файлы),
      `requires_running` (каскад отключения), `uses` (мягкая связь), `core`; тест сверяет граф
      с тем, что реально используют скрипты.
- [x] Тест устойчивости: отключение каждого компонента по очереди не ломает остальные -
      их проверки здоровья проходят, cron и Console работают.

## Этап 6. Вход в Console

- [x] Логин и пароль те же, что у веб-интерфейса Keenetic; проверка через штатную
      авторизацию роутера (`/auth`), пароль в VWARD не хранится.
- [x] Сессия с ограниченным сроком; без входа закрыты API и все данные страницы (оболочка страницы
      без данных загружается - её отдаёт lighttpd напрямую; открыт только `ping` для проверки здоровья).
- [x] Включается настройкой; на время разработки по умолчанию выключено.
- [ ] Формат ответа `/auth` проверить на реальном роутере.
- [ ] Проверить на роутере имена полей WireGuard в RCI (`wireguard.peer`: адрес сервера, трафик,
      рукопожатие): Console показывает «—», если поле называется иначе.

## Приёмка каждого этапа

- ширина экрана от 320 до 1440 px, светлая и тёмная тема;
- нет наложений, обрезанного текста и горизонтальной прокрутки;
- браузерная проверка без ошибок JavaScript;
- тесты репозитория проходят; демо-страница обновлена для просмотра.

Автоматический аудит интерфейса (обход в браузере, как для макета):

- у каждого `data-act` есть обработчик, у каждого обработчика - кнопка; каждая форма
  обрабатывается; каждый `$('id')` существует;
- каждая страница и страница-список достижима из меню; нет переходов на несуществующие страницы;
- «назад» со страницы-списка ведёт в её раздел; каждая запись поиска находит свою строку;
- все кнопки нажимаются без ошибок; Esc закрывает всплывающие панели;
- нет повторяющихся `id`; у кнопок-иконок есть подпись; `role="button"` доступен с клавиатуры
  и срабатывает на Enter и пробел; нет кнопок внутри кнопок;
- заголовок панели не повторяет заголовок страницы; один параметр показан на одной странице.
