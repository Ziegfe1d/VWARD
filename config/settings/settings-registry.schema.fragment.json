{
  "required_change": {
    "path": "properties.settings.items.properties.source.enum",
    "add": ["ads-privacy-guard.conf"],
    "reason": "Ads & Privacy Guard has its own root-owned 0600 component configuration; do not overload update.conf or browser settings."
  },
  "test_change": {
    "path": "tests/repository/check-settings-registry.py",
    "reason": "The current Dev.7 test intentionally permits editable=true only for four update.conf keys. Integration must add a separate allowlist of validated Ads & Privacy Guard keys and keep all unknown/secret keys rejected."
  }
}
